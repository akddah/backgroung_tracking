import 'package:kiwi/kiwi.dart';
import 'Login/LoginBloc/LoginBloc.dart';

void initKiwi() {
  KiwiContainer container = KiwiContainer();

  container.registerFactory((c) => LoginBloc());

  // container.registerFactory((c) => SingleProductBloc());
  // container.registerFactory((c) => AccountActivationBloc());
  // container.registerFactory((c) => CartBloc());
  // container.registerFactory((c) => CommonQuestionsBloc());
  // container.registerFactory((c) => MapServicesBloc());
  // container.registerFactory((c) => AppInitBloc());
  // container.registerFactory((c) => UpdateProfileBloc());
  // container.registerFactory((c) => UpdatePasswordBloc());
  // container.registerFactory((c) => AddProductBloc());
  // container.registerFactory((c) => GetProviderProductBloc());
  // container.registerFactory((c) => AllCategoriesBloc());
  // container.registerFactory((c) => EditProductBloc());
  // container.registerFactory((c) => DeleteNotificationBloc());
  // container.registerFactory((c) => GetProfileBolc());
  // container.registerFactory((c) => DeleteProductBloc());
  // container.registerFactory((c) => AboutUsTermsBloc());
  // container.registerFactory((c) => AddConsultFarmerbolc());
  // container.registerFactory((c) => GetNotificationBloc());
  // container.registerFactory((c) => SliderImageBloc());
  // container.registerFactory((c) => BlogHomeBloc());
  // container.registerFactory((c) => AllProductBloc());
  // container.registerFactory((c) => HomeServicesBloc());
  // container.registerFactory((c) => SearchProductsbloc());
  // container.registerFactory((c) => UpdateProfileClientBloc());
  // container.registerFactory((c) => GetProviderConsultBloc());
  // container.registerFactory((c) => AllBanksBloc());
  // container.registerFactory((c) => AddBankBloc());
  // container.registerFactory((c) => CoponBloc());
  // container.registerFactory((c) => CreatOrderBloc());
  // container.registerFactory((c) => DeleteBankBloc());
  // container.registerFactory((c) => AllChatsBloc());
  // container.registerFactory((c) => CenterBloc());
  // container.registerFactory((c) => OrdersProductBloc());
  // container.registerFactory((c) => ChatDetilsBloc());
  // container.registerFactory((c) => SendMsgBloc());
  // container.registerFactory((c) => ConsultOrderBloc());
  // container.registerFactory((c) => OrdersServicesBloc());
  // container.registerFactory((c) => CancelOrderBloc());
  // container.registerFactory((c) => AddrentBloc());
  // container.registerFactory((c) => DeleteChatBloc());
  // container.registerFactory((c) => OrderProviderProductBloc());
  // container.registerFactory((c) => HelpfullBloc());
  // container.registerFactory((c) => AllBanksBloc());
  // container.registerFactory((c) => AllBanksBloc());
  // container.registerFactory((c) => AllBanksBloc());
}
